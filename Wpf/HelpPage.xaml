<UserControl x:Class="UnboundKeys.Wpf.HelpPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- The manual in one page — only the things the other pages can't
         show for themselves: the spoken commands, what the modes do, the
         safety nets, the on-screen keyboard's tricks and a few facts.
         Built in code (see HelpPage.xaml.cs) so the words come from the
         same constants the app itself listens for. -->
    <StackPanel x:Name="Sections" Margin="0,0,0,12"/>
</UserControl>
