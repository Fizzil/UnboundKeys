namespace UnboundKeys.Wpf;

// The four things the left rail can open. Profile isn't one of them any
// more — it's the rail's footer chip — and Fade is a toggle there too.
public enum DashboardSection
{
    Mouse,
    Keyboard,
    Voice,
    Settings,
}
